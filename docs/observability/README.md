# Observability

- `slo.md` — SLIs, SLOs and the error-budget policy (`PLAN.md` §10), defined in M7 **before** any game day, then referenced throughout M12's experiments. **Not written yet.**

Alarm runbooks live in [`../runbooks/`](../runbooks/) (one per alarm), not here. Dashboard screenshots live in `../screenshots/07-observability/`.
