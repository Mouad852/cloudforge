# Resilience

- `capacity-planning.md` — max sustainable RPS per instance, the saturation point, and scaling thresholds *derived* from load-test data rather than guessed. Written in M13, once the M12 load tests produce real numbers. **Not written yet.**

Failure-injection results live in [`../experiments/`](../experiments/), not here — this folder is about steady-state capacity behavior, not failure behavior.
